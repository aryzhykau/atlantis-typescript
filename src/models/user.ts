export interface IUser {
    email: string
    first_name: string
    last_name: string
    phone_country_code: string
    phone_number: string
    is_authenticated_with_google: boolean
    role: string
    id: number
}
